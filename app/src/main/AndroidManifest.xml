<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ajay.suicide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/breathe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AskForHelpActivity"
            android:label = "HelpLine Numbers"/>
        <activity android:name=".Register"
            android:label="Register"/>

        <activity android:name=".Profile"
            android:label="My Profile"/>
        <activity android:name=".WarningSignsActivity"
            android:label="Warning Signs"/>
        <activity android:name=".ReasonsActivity"
            android:label="Reasons To Live"/>
        <activity android:name=".SafetyActivity"
            android:label="What To Do"/>
        <activity android:name=".PlacesActivity"
            android:label="My Safe Place"/>
    </application>

</manifest>